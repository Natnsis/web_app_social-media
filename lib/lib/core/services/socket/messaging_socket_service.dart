import 'package:faithconnect/core/services/socket/direct_messaging_socket_service.dart';

/// Back-compat alias — `/messaging` is direct (1-to-1) chat only.
typedef MessagingSocketService = DirectMessagingSocketService;

typedef MessagingSocketServiceImpl = DirectMessagingSocketServiceImpl;
